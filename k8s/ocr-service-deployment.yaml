apiVersion: apps/v1
kind: Deployment
metadata:
  name: vaultx-ocr-service
  namespace: vaultx
spec:
  replicas: 2
  selector:
    matchLabels:
      app: vaultx-ocr-service
  template:
    metadata:
      labels:
        app: vaultx-ocr-service
    spec:
      containers:
        - name: ocr-service
          image: ghcr.io/hrushi4151/vaultx-ocr-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8003
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 15
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: vaultx-ocr-service
  namespace: vaultx
spec:
  selector:
    app: vaultx-ocr-service
  ports:
    - port: 8003
      targetPort: 8003
