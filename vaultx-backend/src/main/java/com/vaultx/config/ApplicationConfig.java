package com.vaultx.config;

import org.springframework.context.annotation.Configuration;

/**
 * Application-level Spring configuration.
 * Additional application-wide beans will be registered here as the platform grows.
 */
@Configuration
public class ApplicationConfig {
    // Future application-level beans (e.g., ObjectMapper, MessageSource) go here.
}
